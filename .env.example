PORT=3000
MONGO_URI=mongodb://localhost:27017/sena_chat_db

# Mismo secret que la API principal (ASP.NET) y API Imágenes HM
JWT_SECRET=tu_clave_secreta_jwt

# Para llamadas server-to-server desde .NET (crear sala al crear cita)
API_KEY=tu_api_key_secreta

# CORS - Orígenes permitidos separados por coma
CORS_ORIGINS=http://localhost:4200,http://localhost:3000,http://localhost:5173

# Secreto compartido con la API .NET (appsettings Chat:InternalNotifySecret) para POST /api/chat/internal/notify
CHAT_INTERNAL_NOTIFY_SECRET=genera_un_string_largo_aleatorio
